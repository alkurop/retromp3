plugins {
    id "com.android.library"
    id "kotlin-android"
}

apply from: "$scripts.android_library"

android {
    namespace "${applicationId}.entity"
}

dependencies {
    api project(':entity:data')
    api project(':entity:platform')
}
