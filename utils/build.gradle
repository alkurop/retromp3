plugins {
    id "com.android.library"
    id "kotlin-android"
}

apply from: "$scripts.android_library"

android {
    namespace "${applicationId}.utils"
}

dependencies {
    api project(':utils:data')
    api project(':utils:platform')
}
